using System;
using Mogre;
using PhysicsEng;
using Mogre.TutorialFramework;
using RaceGame;

namespace Mogre.Tutorial
{
    abstract class StaticElement : GameElement
    {
        public virtual void SetPosition()
        {
            /// get { return SetPosition; }
            //robotNode.Translate(position);
             //SetPosition = value; 
       }
    }
}
